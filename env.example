# ============================================================================
# Flask 后端服务 - 环境变量配置示例
# ============================================================================
# 复制此文件为 .env 并修改相应的配置值
# 命令: cp env.example .env  (Linux/Mac) 或 copy env.example .env (Windows)

# ============================================================================
# 基础配置
# ============================================================================

# Flask 运行环境: development 或 production
FLASK_ENV=development

# 密钥（生产环境必须修改！）
SECRET_KEY=your-secret-key-here-change-in-production

# ============================================================================
# CORS 跨域配置（重要！）
# ============================================================================

# 方式一: 开发环境 - 允许所有来源（仅本地开发使用，不推荐）
# CORS_ORIGINS=*

# 方式二: 开发环境 - 指定多个来源（推荐）
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# 方式三: 生产环境 - 指定生产域名（生产环境必须使用）
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================================================
# 数据库配置
# ============================================================================

# 数据库类型选择: 'sqlite' 或 'mysql' (默认: mysql)
DB_TYPE=mysql

# 方式一: 使用 DB_TYPE 配置（推荐）
# ============================================================
# MySQL 配置（当 DB_TYPE=mysql 时）
# ============================================================
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=test

# ============================================================
# SQLite 配置（当 DB_TYPE=sqlite 时）
# ============================================================
# SQLITE_DB_PATH=app.db

# 方式二: 直接设置 DATABASE_URL（会覆盖上面的配置）
# ============================================================
# MySQL
# DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/test?charset=utf8mb4

# SQLite
# DATABASE_URL=sqlite:///app.db

# PostgreSQL 示例
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# ============================================================================
# 邮箱服务配置
# ============================================================================

# 邮件服务器
MAIL_SERVER=smtp.gmail.com

# 邮件端口 (587 for TLS, 465 for SSL, 25 for non-encrypted)
MAIL_PORT=587

# 使用 TLS (推荐)
MAIL_USE_TLS=true

# 使用 SSL (如果使用 465 端口)
MAIL_USE_SSL=false

# 邮箱用户名（发送邮件的邮箱）
MAIL_USERNAME=your_email@gmail.com

# 邮箱密码或授权码
MAIL_PASSWORD=your_password_or_app_password

# 默认发件人（可选，默认使用 MAIL_USERNAME）
MAIL_DEFAULT_SENDER=your_email@gmail.com

