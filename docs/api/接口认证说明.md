# 接口认证说明

## 📋 概述

系统使用 JWT (JSON Web Token) 进行接口认证。需要登录才能访问的接口，必须在请求头中携带有效的 Token。

---

## 🔐 认证方式

### Token 获取

用户登录成功后，会从登录接口返回 `access_token`：

```json
{
  "success": true,
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "token_type": "Bearer",
    "expires_in": 1800
  }
}
```

### Token 使用

在请求需要认证的接口时，需要在请求头中携带 Token。支持两种方式：

**方式一：Authorization Header（推荐）**
```
Authorization: Bearer <access_token>
```

**方式二：X-Access-Token Header**
```
X-Access-Token: <access_token>
```

---

## 📡 请求示例

### 使用 Fetch API

```javascript
const token = localStorage.getItem('access_token');

fetch('http://localhost:5000/api/users/me', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  }
})
.then(response => response.json())
.then(data => console.log(data));
```

### 使用 Axios

```javascript
import axios from 'axios';

const token = localStorage.getItem('access_token');

axios.get('http://localhost:5000/api/users/me', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
})
.then(response => {
  console.log(response.data);
})
.catch(error => {
  console.error(error.response.data);
});
```

### 使用 Axios 拦截器（推荐）

```javascript
import axios from 'axios';

// 创建 axios 实例
const api = axios.create({
  baseURL: 'http://localhost:5000/api',
  timeout: 10000
});

// 请求拦截器：自动添加 Token
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('access_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器：处理 Token 过期
api.interceptors.response.use(
  (response) => {
    return response;
  },
  (error) => {
    if (error.response?.status === 401) {
      const code = error.response.data?.code;
      if (code === 'TOKEN_EXPIRED' || code === 'INVALID_TOKEN') {
        // Token 过期或无效，跳转到登录页
        localStorage.removeItem('access_token');
        localStorage.removeItem('refresh_token');
        window.location.href = '/login';
      }
    }
    return Promise.reject(error);
  }
);

export default api;
```

---

## 🔒 公开接口（不需要认证）

以下接口不需要携带 Token：

- `POST /api/register` - 用户注册
- `POST /api/login` - 用户登录
- `POST /api/refresh-token` - 刷新 Token
- `POST /api/logout` - 用户登出
- `POST /api/send-verification-code` - 发送邮箱验证码
- `POST /api/verify-code` - 验证邮箱验证码
- `GET /api/captcha` - 获取图形验证码
- `GET /api/health` - 健康检查

---

## 🔐 需要认证的接口

除了上述公开接口外，所有 `/api/` 开头的接口都需要携带 Token。

**示例**：
- `GET /api/users/me` - 获取当前用户信息
- `GET /api/users/<id>` - 获取指定用户信息
- `GET /api/users` - 获取用户列表（需要管理员权限）

---

## ⚠️ 错误响应

### 1. 未提供 Token

**状态码**: `401 Unauthorized`

**响应**:
```json
{
  "success": false,
  "message": "未提供 Token，请先登录",
  "code": "NO_TOKEN",
  "detail": "请在请求头中添加 Authorization: Bearer <token> 或 X-Access-Token: <token>"
}
```

**处理方式**：
- 检查请求头是否正确添加了 Token
- 如果 Token 不存在，跳转到登录页面

---

### 2. Token 无效或过期

**状态码**: `401 Unauthorized`

**响应**:
```json
{
  "success": false,
  "message": "Token 已过期",
  "code": "TOKEN_EXPIRED",
  "detail": "Token 无效或已过期，请重新登录"
}
```

或

```json
{
  "success": false,
  "message": "Token 无效: ...",
  "code": "INVALID_TOKEN",
  "detail": "Token 无效或已过期，请重新登录"
}
```

**处理方式**：
- 清除本地存储的 Token
- 尝试使用 `refresh_token` 刷新 Token
- 如果刷新失败，跳转到登录页面

---

### 3. 用户不存在

**状态码**: `401 Unauthorized`

**响应**:
```json
{
  "success": false,
  "message": "用户不存在",
  "code": "USER_NOT_FOUND",
  "detail": "Token 中的用户 ID 不存在于数据库中"
}
```

**处理方式**：
- 清除本地存储的 Token
- 跳转到登录页面

---

### 4. 账户被禁用

**状态码**: `403 Forbidden`

**响应**:
```json
{
  "success": false,
  "message": "账户已被禁用，请联系管理员",
  "code": "USER_BANNED",
  "detail": "您的账户已被管理员禁用，无法访问系统"
}
```

**处理方式**：
- 清除本地存储的 Token
- 提示用户联系管理员
- 跳转到登录页面或未授权页面

---

## 🔄 Token 刷新流程

当 Access Token 过期时，可以使用 Refresh Token 刷新：

```javascript
// 刷新 Token
async function refreshAccessToken() {
  const refreshToken = localStorage.getItem('refresh_token');
  
  try {
    const response = await fetch('http://localhost:5000/api/refresh-token', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        refresh_token: refreshToken
      })
    });
    
    const data = await response.json();
    
    if (data.success) {
      // 更新 Access Token
      localStorage.setItem('access_token', data.data.access_token);
      return data.data.access_token;
    } else {
      // 刷新失败，跳转到登录页
      localStorage.clear();
      window.location.href = '/login';
    }
  } catch (error) {
    console.error('刷新 Token 失败:', error);
    localStorage.clear();
    window.location.href = '/login';
  }
}

// 在请求拦截器中使用
api.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;
    
    // 如果是 401 错误且 Token 过期，尝试刷新
    if (error.response?.status === 401 && 
        error.response.data?.code === 'TOKEN_EXPIRED' &&
        !originalRequest._retry) {
      
      originalRequest._retry = true;
      
      try {
        const newToken = await refreshAccessToken();
        
        // 使用新 Token 重试原请求
        originalRequest.headers.Authorization = `Bearer ${newToken}`;
        return api(originalRequest);
      } catch (refreshError) {
        // 刷新失败，跳转到登录页
        window.location.href = '/login';
        return Promise.reject(refreshError);
      }
    }
    
    return Promise.reject(error);
  }
);
```

---

## 📝 最佳实践

### 1. Token 存储

- 推荐使用 `localStorage` 或 `sessionStorage` 存储 Token
- 不要将 Token 存储在 Cookie 中（除非有特殊需求）
- 敏感信息不要存储在客户端

### 2. Token 安全

- Token 不应该暴露在 URL 中
- 不要在日志中打印完整的 Token
- 使用 HTTPS 传输 Token（生产环境）

### 3. 错误处理

- 统一处理 401 错误（Token 过期或无效）
- 自动刷新 Token 机制
- 友好的错误提示

### 4. 请求拦截器

- 使用 Axios 拦截器自动添加 Token
- 统一处理响应错误
- 实现 Token 自动刷新

---

## 🔗 相关文档

- [登录接口文档](./API.md)
- [用户权限接口文档](./用户权限接口文档.md)

